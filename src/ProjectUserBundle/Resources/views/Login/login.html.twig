<div class="connected-block">





</div>

<div class="collapse navbar-collapse dark-blue" >
    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
        <ul class="nav navbar-nav navbar-left">
                <li>
                        <a href="#">Daysound</a>
                </li>
                <li>
                        <a class="navbar-brand"><img src="{{ vich_uploader_asset(app.user, 'imageFile') }}" width="25" height="25" /></a>
                </li>
                <li>
                        <p class="navbar-text">{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }}</p>
                </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Profil <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                                <li><a href="#">Mon profil</a></li>
                                <li><a href="{{ path("fos_user_profile_show") }}">settings</a></li>
                                <li> <a href="{{ path('fos_user_security_logout') }}">
                                        {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                                        </a>
                                </li>

                        </ul>
                </li>
        </ul>
    {% else %}
        <ul class="nav navbar-nav navbar-left">
                <li>
                        <a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                </li>
                <li>
                        <a href="{{ path('fos_user_security_sign_up') }}"></a>

                </li>
        </ul>
    {% endif %}
        <form class="navbar-form navbar-right" role="search">
                <div class="form-group">
                        <input type="text" class="form-control" placeholder="Search">
                </div>
                <button type="submit" class="btn btn-default">Submit</button>
        </form>

</div>